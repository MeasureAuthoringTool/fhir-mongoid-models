RSpec.describe Fhir::Mongoid::Models do
  it "has a version number" do
    expect(Fhir::Mongoid::Models::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
